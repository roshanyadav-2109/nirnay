# Supabase project (already provisioned for the demo)
VITE_SUPABASE_URL=https://acjwagnrdbkedvacligr.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: set a default Gemini API key for local dev.
# In the deployed app the user enters their key in the Settings panel
# and it is stored in browser localStorage only.
# Get a free key at https://aistudio.google.com/apikey
VITE_GEMINI_API_KEY=
